# API Configuration
# Override the default API URLs if needed
API_BASE_URL=https://your-api-url.com

# Development (uses ngrok by default)
DEV_API_URL=https://your-dev-api.ngrok.app

# Production
PROD_API_URL=https://api.yourdomain.com
